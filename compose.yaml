services:
  tuya-home-assistant-ir-bridge:
    build: .
    container_name: tuya-home-assistant-ir-bridge
    restart: unless-stopped
    network_mode: host
    env_file:
      - backend/.env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
